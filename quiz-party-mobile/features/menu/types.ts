// Базовые типы для native-меню.
// Если хочешь быстро понять, какие данные вообще живут в этом экране,
// начинать удобно именно с этого файла.

export type MenuProfile = {
  // Имя игрока, которое показывается в баннере профиля.
  name: string;

  // Выбранный эмодзи-аватар игрока.
  emoji: string;
};

// Режим модалки профиля.
// `create` — первый обязательный ввод профиля.
// `edit` — последующее редактирование уже созданного профиля.
export type ProfileModalMode = 'create' | 'edit';
