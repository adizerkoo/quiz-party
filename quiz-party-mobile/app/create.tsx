// Native-экран создания квиза.
// Здесь живёт полный перенос create.html без WebView-обёртки.
import { NativeCreateScreen } from '@/features/create';

export default function CreateScreen() {
  return <NativeCreateScreen />;
}
